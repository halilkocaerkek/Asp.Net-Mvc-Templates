using DataModel;

public partial interface IScheduledataRepository : IRepository<ScheduleData>
{
    //Add any additional repository methods other than the generic ones (GetAll, GetById, Delete, Add)
}
